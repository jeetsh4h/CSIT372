/*
 * The command for compiling this file
 * `g++ -Wall -Wpedantic -Werror *.cpp -o db_test.out && ./db_test.out`
 * Make sure the above command is run in the same directory
 * as the file main.cpp
 */

#include "db-cmds.hpp"

int main() {
    dbLoop();
}
